記録

問題点　事項
JsonObject のパッケージ問題

C# のソースコード中のメソッドを返してるところで、
Task<string>となっていて、メソッドの戻り値をもらう時に、
Task＜ String＞の中身の取り出しがわかなくて、中々取り出せない。

C#の非同期処理などが、理解できていない。
C＃の情報量がJAVAに比べて少ないと感じている。
調べ方の問題も考えれれる。

SimpleHttpRequstは、ソースコード完了。
テストは、できていない。
HttpClientのクラスを使おうとしたが、使えないため、HttpWebRequestクラスを使用した。

